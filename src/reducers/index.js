// import types from '../types';

const INITIAL_STATE = {};

export default (state = INITIAL_STATE, action) => {
  // switch (action.type) {
  // 	case types.SOME_TYPE:
  // 		return { ...state }
  // 	default:
  // 		return state
  // }
  return state;
};
